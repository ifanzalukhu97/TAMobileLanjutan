<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidtutorialshub.tamobilelanjutan">

    <!--TODO 1 Add user permission-->
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CarouselViewActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".ContainerActivity" />
        <activity android:name=".GantiPINActivity" />
        <activity android:name=".MutasiSaldoActivity"></activity>
    </application>

</manifest>